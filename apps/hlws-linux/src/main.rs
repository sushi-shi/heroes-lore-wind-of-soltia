//! `hlws-linux` — a native host that plays the strict Heroes Lore: Wind of
//! Soltia transliteration.
//!
//! Phase-3 skeleton: it links the game
//! (`heroes-lore-wind-of-soltia-game-xlat`), the device runtime (`j2me-me`), the
//! JVM layer (`j2me-jvm`), and the windowing deps (`winit`, `softbuffer`) so the
//! workspace's app shell exists and builds. The window open + frame loop +
//! present land in a later phase.

fn main() {}
