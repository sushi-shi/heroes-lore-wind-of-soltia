# Local EXACT-pixel agreement ratchet for the first-frame differential oracle
# (tests/first_frame_oracle.rs). Distinct from tools/oracle/agreement.toml, the
# Python route comparator's ratchet — this one is the Rust test's own, read at
# runtime. `differing_pixels` is counted over RGB only (alpha masked, & 0x00FFFFFF),
# matching tools/oracle/compare_frames.py::compare_exact.
#
# A recorded number can only move DELIBERATELY: the test asserts equality, so it
# fails on a regression AND on an unrecorded improvement (stalker's ratchet
# pattern). Never edit a number just to make a change pass — look at the diff
# image the run writes to _temp/oracle/first_frame/ first.
#
# key = recorded differing_pixels for the port's seated state-10 frame vs each
#       captured reference label (pass-1/00-boot/<label>.png).

# The port's FIRST RENDERED FRAME is the HANDS-ON MOBILE publisher splash
# (state-10 logo animation, logoFrames[4]). It is PIXEL-EXACT to the real v2.0.7
# game's captured publisher-splash frame: 0 differing pixels. This is the proof.
publisher-splash = 0

# The state-1 HEROES LORE title screen, now RENDERED by the port (TitleScreen.paint
# case 1: title art `titleBgFrames[2..4]`, version "2.0.7", the two RNG-driven
# fluttering birds `titleMenuFrames`, and the "PRESS ANY KEY" footer). Driven to
# the animation moment the capture took — the 42nd state-1 paint (sim k=41), the
# pixel-diff minimum across the settled sweep (next-best 202 px); the birds match
# because `Game.byte_util` is seeded to the route's 305419896 before startTitle.
#
# The residual 136 px is EXACTLY the "PRESS ANY KEY" footer glyphs, and only their
# COLOUR: the game bytecode draws the footer with `graphics.setColor(0)` (black) →
# fontForColor(0) → smallBlack, so the faithful port renders those 136 glyph pixels
# BLACK. The FreeJ2ME capture renders them ORANGE (255,136,0) — the smallOrange
# ink left over from the immediately-preceding version-text `setColor(4136767)` —
# i.e. FreeJ2ME's `setColor(0)`/getColor does not take, an EMULATOR artifact, not
# game logic. Matching it would require an unfaithful hack, so the port stays
# correct-per-MIDP and this 136 is recorded honestly. Everything else — art,
# version text, both birds, footer POSITION/GLYPHS — is pixel-exact (0 diff).
title-logo = 136

# =====================================================================
# MAIN MENU (tests/menu_oracle.rs) — the boot → title → any-key → parchment
# main menu, then DOWN-navigation. Driven through the REAL path:
# TitleScreen.keyPressed (state 1) → enterStoryMode → run() loadPhase-2 →
# GameLoop.showGameScreen → GameState.buildLoadMenu → GameScreen.paint (screen 9)
# → MainMenu.draw → MainMenu.paint. The port was PREVIOUSLY stuck on the
# title/publisher, so these parchment-menu references diffed at ~99% (~75865 px).
#
# Now the whole menu renders: the dark-violet ground (4136767), the parchment
# panel (drawMenuPanel over menuFrames[3..13] + the 16763769 fill), the red
# selection sprite (menuFrames[18]), the six big-font item labels, and the
# soft keys. Navigation is exact: DOWN from NEW GAME (cursorIndex 0) SKIPS the
# disabled LOAD row (index 1) straight to OPTIONS (2) → HELP (3) → ABOUT (4) →
# EXIT (5) — the fresh-install no-save order.
#
# THE RESIDUAL IS 100% THE FreeJ2ME getColor/setColor COLOUR-LATCH — the SAME
# emulator artifact as the title-footer 136 above, now on the WHITE-fill text:
#   * the HIGHLIGHTED item (drawMenuItem: itemState even → setColor(16777215) →
#     fontForColor → bigWhite) is rendered by the faithful port as bright WHITE
#     (white fill / black outline); FreeJ2ME's stale getColor picks bigBlack, so
#     the capture shows the selected label DARK. (~370 px on NEW GAME, less on
#     shorter labels.)
#   * the soft keys "Ok"/"Exit" (drawSoftKeys: setColor(16777215) → smallWhite)
#     are drawn WHITE by the port; FreeJ2ME renders them #ff8800 (smallOrange) —
#     the ~57 px present in every menu frame.
# The (port_rgb -> ref_rgb) histogram is ONLY black<->white and white->#ff8800
# pairs — there is NO positional/shape/geometry diff. Everything else (panel,
# sprite, ALL the black unhighlighted labels, layout) is pixel-EXACT (0 diff).
# Matching the capture would require reproducing the emulator's colour bug (an
# unfaithful hack, forbidden by docs/TRANSLITERATION.md), so the port stays
# correct-per-MIDP and these residuals are recorded honestly.
#
# 00-boot/main-menu-new-game and 01-menu/menu-new-game are the SAME captured
# frame (one SOFT1 from the title), so both pin the same port framebuffer = 427.
main-menu-new-game = 427
menu-new-game = 427
menu-options = 376
menu-help = 240
menu-about = 297
menu-exit = 232
